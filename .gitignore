# ===== 用户配置与凭据：绝对不要提交 =====
# 里面有 Emby 地址、用户名、密码 / API 令牌、MetaTube 地址等
config.json

# ===== 运行时缓存 =====
# gfriends 索引约 10 MB，首次启动会自动重新下载
cache/

# ===== 工具内部数据 =====
.workbuddy-ai/

# ===== 真实环境截图 =====
# 只保留 01–09 这批 mock 环境截图（README 引用的就是它们）。
# 其余一律不入库 —— 10 号之后都是在真实 Emby 上截的，含真实媒体库内容。
# 用「白名单」写法而不是逐个列黑名单：以后新截图默认被忽略，不会漏。
screenshots/*
!screenshots/01-*.png
!screenshots/02-*.png
!screenshots/03-*.png
!screenshots/04-*.png
!screenshots/05-*.png
!screenshots/06-*.png
!screenshots/07-*.png
!screenshots/08-*.png
!screenshots/09-*.png

# ===== 构建副产物 =====
# 正式产物是 EmbyMetaEditor.exe（构建命令见 README）。
# 顺手跑 `go build ./...` 会按模块名再生成一个不带 -trimpath 的，别提交进来。
emby-meta-editor.exe

# ===== 编辑器 / 系统产生的备份与杂物 =====
*~
*.exe~
*.bak
*.orig
*.tmp

# Python 脚本（tools/）跑过之后的字节码缓存
__pycache__/
*.pyc

# ===== 操作系统文件 =====
Thumbs.db
Desktop.ini
.DS_Store
